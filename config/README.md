# Config Directory

Folder ini berisi file konfigurasi untuk aplikasi WhatsApp Bot.

## File Konfigurasi

- `config.js`: Konfigurasi utama aplikasi
- `.env`: File environment variables (tidak disimpan di git)
- `.env.example`: Contoh file environment variables